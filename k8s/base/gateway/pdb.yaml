apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: weaver-gateway-pdb
  labels:
    app: weaver-ai-gateway
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: weaver-ai-gateway
